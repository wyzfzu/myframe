<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.myframe.mapper.UserMapper">

    <select id="selectCount2" resultType="java.lang.Integer" parameterType="map">
        select count(1) from t_test_user
        where user_name like #{userName}
    </select>

    <select id="selectOne2" resultType="com.myframe.pojo.User" parameterType="map">
        select id as id, user_name as userName, birthday
        from t_test_user
        where id = #{id}
    </select>

</mapper>